name = "api2skill-oauth-relay"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Optional multi-isolate persistence (uncomment + create KV namespace):
# [[kv_namespaces]]
# binding = "SESSIONS"
# id = "REPLACE_WITH_KV_ID"
# preview_id = "REPLACE_WITH_PREVIEW_KV_ID"

[vars]
# Override public callback host if behind a custom domain:
# PUBLIC_BASE = "https://oauth.api2skill.dev"

[dev]
port = 8787
local_protocol = "http"
