# Install Report — api2skill

Generated by `.agents adapt` (greenfield repo).

## Detection

| Source | Signal | Weight | Notes |
|---|---|---|---|
| PROJECT.md (Stack) | `C# / .NET (console application)` | 2 | Primary signal — greenfield, no runtime files yet |
| Human override | `--dotnet-console-app` | 2 | Confirms PROJECT.md, no conflict |
| codesearch MCP | 5 files / 21 chunks indexed | — | All pipeline scaffolding (`AGENTS.md`, `CLAUDE.md`, `pipeline.manifest.json`, skill docs) — no application code, confirms greenfield |
| Filesystem | no `*.csproj` / `*.sln` | — | Expected pre-scaffold |

**Runtime pack:** `dotnet-minimal` (score 0.9) — only .NET pack in the registry; no
dedicated "console app" pack exists, but its `developer` agent + `dotnet-code-style` rule
and `dotnet build`/`dotnet test` defaults apply equally to a console app.

**Add-on packs:** none — no Postgres/EF, Helm/K8s, Node, or iOS signals present.

**Agents:** core six (coordinator, ba-analyst, architect, team-lead, developer, tester) +
`security-reviewer` (enabled — PROJECT.md Open Questions calls out API key / Bearer-OAuth2 /
Basic auth schemes to support, which is an auth/token-handling signal) + `tech-writer`
(enabled — `default_lane: full`). `devops` disabled — no CI/CD target chosen yet
(`PROJECT.md` Delivery: "CI/CD: none yet").

**Hosts:** both `cursor` and `claude` requested by human override (`--use-cursor
--use-claude`). Both already present from `agentic-tool install --host all`.
`pipeline.manifest.json` → `host: "all"`.

**Spec Kit:** already initialized with `--integration claude` (skills-based:
`.claude/skills/speckit-*`). Added `cursor-agent` integration via
`specify integration install cursor-agent` so `.cursor/skills/speckit-*` is registered too.
`specify integration status` → both installed, 0 modified/missing managed files.

**Confidence:** `high` — stack is explicit in both `PROJECT.md` and the human override, and
only one .NET pack exists in the registry (no ambiguity).

## Next steps

1. Human confirms this recommendation (or edits `PROJECT.md` / manifest overrides and
   re-runs stack-selector).
2. `agentic-tool apply --target .`
3. Seed `.specify/memory/constitution.md` via `/speckit.constitution` from `PROJECT.md`.
4. `agentic-tool sync --target . --host auto` then `agentic-tool verify --target .`
